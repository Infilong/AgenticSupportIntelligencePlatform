# Local four-pipeline verification

## Execution brief

User outcome: complete M5/M6 after repairing routine answers, human intervention and noise routing.
This slice connects the existing frozen comparison to real local generation (EVAL, RAG, LANG, TRACE).
No paid API, new infrastructure, fixture tuning or relabelling of manual contributions is authorized.

Root owns implementation. Add an explicit versioned local mode with admission-time model settings;
keep manual comparisons compatible. Direct has no retrieval/context sources. Vector and hybrid use
their declared retrieval; system uses the real persisted support graph. Persist model dispatch before
inference, preserve failures/uncertainty, fence publication and retain actual usage without attributing
machine output to a human. Run the fixed 30 cases across all four pipelines and retain all failures.

Verify mode isolation, no-retrieval direct, cancellation, replay and permission denial on PostgreSQL;
run affected provider/frontend checks, inspect the browser and obtain independent source review.
Record semantic review separately from structural checks. Baseline failures remain visible and do not
justify lowering frozen targets. Then refresh M6 regression, packaged runtime and recovery evidence.

Routing prerequisite f4c998b is pushed. All56 prep checks pass at
`.artifacts/m0/prep-20260910T050650533799Z`; the preceding sandboxed attempt could not terminate its
test child and failed. That original failure remains in `.artifacts/m0/prep-20260910T050607625439Z`.


## Focused checkpoint, full batch pending

Explicit local_ollama comparison admission captures model/endpoint; manual default compatibility
remains. Quality exposes provider selection and machine attribution. Separate local runner/scorer
versions preserve all30 cases, five semantic checks and90%/80% targets, with current user-approved
routine-answer and meaningless-input routing only. No human contributor is fabricated.

Focused18 PostgreSQL checks pass52.04s at
`.artifacts/m0/local-comparisons-api-20260910T051903805450Z`. Earlier two setup failures and a
subsequent cancellation expectation failure (HTTP409) remain retained. Final16 provider units
pass1.53s; frontend build/48 tests pass8.71s; local plus legacy scorer15 checks pass0.19s.
Routing prerequisite CI 34439841295 passed. At that earlier checkpoint, the actual batch at `.artifacts/m5/local-live-20260910`
was still running, including preserved direct-format failures. This is not a full-corpus pass or
M5/M6 completion. Independent review and final source-bound evidence are still required.

## Local batch and release evidence — 2026-09-10

The fixed batch `.artifacts/m5/local-live-fixed-20260910/reviewed-score.json` has120 technically
completed outcomes with response-bound judgments from three independent language reviewers.
Composite successes: direct7/30, vector22/30, hybrid20/30, system20/30. EN2/8/9/9, JA2/8/6/7,
ZH3/6/5/4 respectively, each out of10. Every frozen local quality target FAILED. Unsupported
claims, omitted conflicts, language and routing errors remain visible. System24 completed and6
awaited review; terminal execution is not semantic success. Initial batch
`.artifacts/m5/local-live-20260910` retains30 direct-format failures and90 terminal RAG outcomes.
An explicit JSON-string example repaired direct formatting; no retrieval tuning was added.

117 units passed47.18s and final lint passes. Full PostgreSQL
`.artifacts/m0/integration-20260910T053351305052Z` timed out420s after138 displayed passes;
it is incomplete, not a pass. Packaged local-mode runtime built/became healthy at
`.artifacts/m0/release-up-20260910T054054753277Z`. Optional release model/mode/URL keys retain
manual mode by default. Chrome provider selection/new local comparison and chrome-comparison.png
were inspected at the baseline viewport; no overlap observed by the coordinator.

Fresh restore `.artifacts/m6/restore-20260910T054137Z/report.json` passed 25-table parity and a new
clone API/worker clarification with 3 checkpoints/0 model calls; wrapper `.artifacts/m0/restore-20260910T054135417022Z`. No restored
semantic-generation/browser claim. M5 evaluation executed but quality acceptance failed;
M6 native zoom and broader release/security evidence remain incomplete. Continue bounded M6
checks without lowering targets or starting speculative RAG perfection work.


## Packaged profile and narrow routing follow-up

`.artifacts/m6/local-release-final/profile.json` records five packaged operator sessions:
EN/JA automatic answers, a Chinese routine-timing question incorrectly routed to review,
missing-support intervention and set-aside. Viewer denial403 and foreign-workspace404 were
observed. End-to-end times38.7–54.3s describe this five-session sample, not simultaneous inference
or an SLO. Initial probe failure was HTTP-client lifecycle misuse, repaired in the probe; it
was not an application defect.

The Chinese false review exposed the broad polite-marker regex matching 请求/申请. The backstop
now matches personal requests or explicit polite sensitive actions, with localized reasons;
22 provider/routing units pass1.65s. Independent documentation reviewer inspected this narrow
source delta; live confirmation was pending at that checkpoint and is recorded below. The earlier120-observation scores are
historical evidence for their exact source, not a remeasurement after this repair. Keep the
Windows420s regression failure visible and inspect the next hosted full PostgreSQL result.


## Live repair verification after checkpoint86deae5

`.artifacts/m6/local-release-final/polite-routing-retry.json` records run
1bd0013a-f2e4-4616-a4b1-67e0b20f857c completed/answered in Chinese, correctly distinguishing
two business days and 21 calendar days, with two citations. Its Qwen7B call records1984 input
tokens and 10174.7589ms generation. The earlier cancelled attempt remains in history. Chrome
screenshot chrome-chinese-answered.png was visually inspected by the coordinator without overlap;
the user tab remains on localhost8011. Both latest development and packaged startup became
healthy; packaged evidence `.artifacts/m0/release-up-20260910T054838297425Z`.

86deae5 is pushed. CI 34442804796 subsequently completed successfully: all five jobs passed, including the full
real PostgreSQL regression. Keep this hosted result separate from the retained local 420s timeout. Prior 120 semantic scores were not rerun by this one repair.
The user subsequently explicitly approved: “Finish the demo; document and defer remaining
quality/release gaps.” M5/M6 close under bounded demo acceptance. All frozen failed targets and
strict production-release gaps remain unchanged and deferred. Source CI is verified; final documentation
checkpoint remains; no further tuning or production-readiness claim follows.


## Completion disposition

Source 86deae5 hosted CI 34442804796 passed all five jobs. The user-approved demo scope is
complete; the failed frozen quality targets and documented strict-release gaps are deferred,
not passed. This record moves to completed under the development protocol. Final documentation
review/checkpoint does not introduce runtime changes or imply strict production readiness.


Final preparation verification: `.artifacts/m0/prep-20260910T060256114259Z` failed because
the moved plan was not yet staged in Git inventory and sandbox process-cleanup access failed.
That evidence is preserved. After staging the rename and granting process access,
`.artifacts/m0/prep-20260910T060342912465Z` passed 56 tests in 16.487s. No source or assertion
changed between these preparation attempts.
