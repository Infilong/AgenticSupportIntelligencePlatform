# Packaged multilingual review journeys

## Execution brief

REVIEW/ROUTE/TRACE/UX: exercise the existing full workbench and workflow-inspector browser journeys
against built release assets, beyond the no-API clarification smoke and pre-generation workload.
Normal verification slice; root owns only two harness configuration changes and evidence/docs.
Read frontend/infra/docs guides. Use the established ASI_APP_BASE_URL/ASI_DEMO_CREDENTIALS
overrides from knowledge.spec.ts, retaining development defaults. No app/provider/schema change.

Run real release retrieval with its existing long synthetic policies and seeded isolated accounts.
Development drafts are explicit fixed test contributions, not API/semantic-generation proof.
Verify EN/JA/ZH cited drafts, exact sources, approve/edit/reject, clipboard, exception permissions,
clarification, cancellation/retry, history, responsive layouts and injected polling outage recovery.
Run existing workflow inspection too; inspect screenshots, logs and status evidence. This is
standard project-local headless browser QA; Computer Use remains stopped and native zoom remains
unverified. Do not attempt to work around its observation policy.

Verify standalone harness types, actual browser journeys, independent diff/evidence review,
owning docs/freshness/preparation, then commit/push. Repair only observed defects. Preserve failed
evidence and existing records. Stop at14:58 UTC; no RAG tuning or paid calls.

## Evidence

From pushed recovery checkpoint5f5a869; its hosted CI34357850431 passed. The built release serves
the same product assets as855d1e1; later changes are verification helpers, CI and documentation.
Standalone strict TypeScript check passes for both changed harnesses; no UI/runtime code changed.

Initial `.artifacts/m6/release-workbench-20260909`: seven passes, three failures in116.65s.
All three failed a raw-string fact assertion because the long handbook contains
`14 calendar\ndays`; retrieval returned the correct REFUND-STANDARD section. Changed only that
assertion to `/14 calendar\s+days/`. Submitted quote bytes and source/provenance checks remain
unchanged. Independent review confirms the repair preserves the required fact and provenance.

Affected rerun `.artifacts/m6/release-workbench-fixed-20260909`: English approve/copy and Japanese
edit/copy pass; Chinese stops at sign-in with429, because repeated tests reused the seeded account.
The account counter was11 in its existing15-minute window; no password or counter repair is
appropriate. Retain the failure and wait for normal expiry before the remaining focused case.
The read-only counter observation gave expiry13:49:43 UTC. The remaining journey was pending at that observation.

After normal expiry, `.artifacts/m6/release-workbench-zh-final-20260909` passes the Chinese
retrieval/draft/source/reject/history case in17.3s. Root inspected the rejected-history screenshot.
Combined evidence covers all ten cases: seven initial passes, two repaired EN/JA passes and this
focused Chinese pass. This is not one final all-green ten-case run. No counters were reset.
Strict standalone TypeScript checking also passes after the assertion repair. The bounded API
log summary in the final directory records465 HTTP200,29 HTTP202,6 HTTP204 and one expected
login429, with no HTTP5xx. The injected polling503 is client-side and does not reach the API.

Inspected mobile Japanese clarification and approved-copy screenshots, plus the expanded mobile
workflow/real model records. Existing checks cover360/768/1440 widths and keyboard source focus.
Repeated policies in the synthetic release corpus are preserved, so this is a connected workflow
check rather than a pristine retrieval benchmark. Development answers remain fixed test inputs;
no external generation, semantic-answer quality or native browser zoom is established.

Independent code/evidence and semantic-documentation reviews closed with no actionable defects.
All46 preparation checks pass at .artifacts/m0/prep-20260909T135242955476Z; the Chromium environment
check also passes at .artifacts/m0/browser-20260909T135157664434Z. This completion note follows
the unchanged-source verification and changes no runtime or mapped documentation contract.
