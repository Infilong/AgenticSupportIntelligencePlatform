# Run python scripts/manage.py init-env to create ignored .env with a random credential.
# Compose project is fixed to asi-rebuild-v1 by the runtime command.
ASI_DATABASE_PASSWORD=
ASI_WEB_PORT=5180
ASI_API_PORT=8010
ASI_DATABASE_PORT=5440
ASI_PROVIDER_MODE=mock
ASI_LIVE_BUDGET_USD=0
# Supply privately when live processing is authorized. Never commit real keys.
OPENAI_API_KEY=
