{
  "name": "asi-rebuild-frontend",
  "version": "0.0.0",
  "private": true,
  "description": "M0 browser verification; product frontend starts in M1",
  "scripts": {
    "test:environment": "playwright test tests/e2e/environment.spec.ts"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0"
  }
}
